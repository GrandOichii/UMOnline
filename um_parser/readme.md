# Board game text parser

__um_parser__ is a client for editing board and card game text parsers, turning game text into Lua code.

## How to run

To build the Rust project, navigate to the _rust_ directory and run
```sh
cargo build
```

Then open the Godot 4.5 project in the _godot_ directory and click __Run project__.